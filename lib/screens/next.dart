import 'package:flutter/material.dart';

class Next extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Next Page'),
      ),
      body: Text('This is just in place to make the button functionable'),
    );
  }
}
